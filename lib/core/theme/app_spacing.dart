/// Sistema de espaçamento base 8px — SKILL §7.
class AppSpacing {
  AppSpacing._();

  static const double xs = 4; // gaps mínimos (ícone+label de nav)
  static const double sm = 8; // gaps próximos
  static const double md = 16; // padding interno de cards menores
  static const double lg = 24; // padding interno de cards grandes, margens
  static const double xl = 32; // gaps grandes, padding horizontal de botão
  static const double xxl = 40; // raio do hero, gaps muito grandes
  static const double huge = 48; // radius do bottom nav, top margins

  /// Margem lateral padrão de tela (~20–24px).
  static const double pagePadding = 20;
}
